import AppLayout from "../../components/layout/AppLayout";
import CheckPoints from "../../components/projects/CheckPoints";

const CheckPointPage = () => {
  return (
    <AppLayout>
      <CheckPoints />;
    </AppLayout>
  );
};

export default CheckPointPage;
